import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";

export const technicians = sqliteTable("technicians", { id: integer("id").primaryKey({ autoIncrement: true }), name: text("name").notNull(), active: integer("active", { mode: "boolean" }).notNull().default(true) });
export const jobs = sqliteTable("jobs", { id: text("id").primaryKey(), patient: text("patient").notNull(), clinic: text("clinic").notNull(), workType: text("work_type").notNull(), teeth: text("teeth"), technicianId: integer("technician_id").references(() => technicians.id), status: text("status").notNull(), receivedAt: integer("received_at", { mode: "timestamp" }).notNull(), dueAt: integer("due_at", { mode: "timestamp" }).notNull(), price: integer("price").notNull().default(0), bonus: integer("bonus").notNull().default(0) });
export const inventory = sqliteTable("inventory", { id: integer("id").primaryKey({ autoIncrement: true }), sku: text("sku").notNull().unique(), name: text("name").notNull(), unit: text("unit").notNull(), quantity: real("quantity").notNull().default(0), minimum: real("minimum").notNull().default(0), unitCost: integer("unit_cost").notNull().default(0) });
export const stockMovements = sqliteTable("stock_movements", { id: integer("id").primaryKey({ autoIncrement: true }), inventoryId: integer("inventory_id").notNull().references(() => inventory.id), type: text("type", { enum: ["IN", "OUT"] }).notNull(), quantity: real("quantity").notNull(), jobId: text("job_id").references(() => jobs.id), note: text("note"), createdAt: integer("created_at", { mode: "timestamp" }).notNull() });
export const transactions = sqliteTable("transactions", { id: integer("id").primaryKey({ autoIncrement: true }), type: text("type", { enum: ["INCOME", "EXPENSE", "BONUS"] }).notNull(), category: text("category").notNull(), amount: integer("amount").notNull(), jobId: text("job_id").references(() => jobs.id), note: text("note"), occurredAt: integer("occurred_at", { mode: "timestamp" }).notNull() });
